// See config.example.js for why this file is committed (the backend URL
// isn't a secret — the browser calls it directly regardless).
window.SAVORA_API_URL = "https://ai-beginner-task-backend-production.up.railway.app";
